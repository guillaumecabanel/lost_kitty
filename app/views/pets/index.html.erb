<h1>Lost Kitty</h1>

<ul class="list-group">
  <% @pets.each do |pet| %>
    <li class="list-group-item">
      <%= pet.name %>
      <%= link_to 'Details', pet_path(pet.id) %>
    </li>
  <% end %>
</ul>
